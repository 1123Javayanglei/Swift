/*:
 ## 列表
 
 你在学习字符串时，已经进行了一些处理事物列表的练习。你通过使用 `\n` 换行字符将多个字符串连接在一起，来制作这些列表。 
 
 ```
(示例): 
 let shoppingList = "鸡蛋" + "\n" + "西红柿" + "\n" + ...
 ```
 
 这可提供一个便于在屏幕上进行阅读的列表，但它除此之外别无用处。想一想还可以使用列表来做的一些事情：
 
 - 第一件事是什么？最后一件事呢？第 24 件事呢？ 
 - 有多少件事情？
 - 可以如何重新安排列表？
 - 如果列表不是由 `String` 值组成的，而是由其他值组成的会怎样？
 
 有一个特殊的类型叫做**数组**，它用于存放项目列表。在此 playground 中，你将学习如何创建和使用数组。  
*/
