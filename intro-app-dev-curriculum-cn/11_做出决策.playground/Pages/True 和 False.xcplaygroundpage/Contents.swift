/*:
 ## True 和 False
 
 在“20 个问题”游戏中，你只可以通过问答案为*“是* 或 *否”*这样的问题来猜出它是什么东西。答案简单的问题功能可能非常强大。
 
 你可以编写可看成是问题的代码，Swift 会告诉你答案是 “true”还是 “false”。你可以像输入计算一样，在边栏中查看结果。
 
 事实上，正如你所看到的那样，`true` 和 `false` 在 Swift 中是特殊值：
*/
true
false
//: 它们可以像数字或字符串一样赋给常量：
let yes = true
是
let no = false
否
//: `true` 和 `false` 值称为“布尔值”，是以 19 世纪数学家 George Boole 的名字命名的，他意识到问题清晰、答案简单非常重要。Swift 中使用 `Bool` 类型来代表布尔值。`Bool` 值只能是 true 和 false。下列代码行会产生错误：
//let perhaps = maybe
//: 如果答案是 `true` 和 `false`，应该如何问问题呢？
//:
//: [上一页](@previous)  |  第 2 页，共 13 页  |  [下一页：等式](@next)
