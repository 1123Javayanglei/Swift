/*:
 ## 总结
 
 在本课中，你知道了特定类型的每个值称为实例。
 
 你还学习了类型不仅仅是 `String` 或 `Int` 等标签，每个类型还可以具有其他功能。
 
 类型可以有属性，它允许每个实例有自己的值集。
 
 类型可以有实例方法，它们是定义为类型一部分的函数。
 
 你可以使用 Xcode 中内建的文档来了解可用的方法和属性。通过按住 Option 键同时点按鼠标代码或使用“Quick Help”检查器，可直接从代码访问文档。也可以在单独的窗口中启动文档浏览器，然后查找你感兴趣的任何内容。
 
 你已经知道，即使看起来非常简单的类型（如 `String`）也有大量方法和属性。
 
 你已经了解为什么将事物作为实例方法和属性提供要比作为在顶层定义的函数提供更有意义。
 
 现在，该通过练习加以实践了。

[上一页](@previous)  |  第 14 页，共 17 页  |  [下一页：练习：BoogieBot 归来](@next)
*/

