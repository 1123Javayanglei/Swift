/*:
 ## 实例
 
 你已经使用了 Swift 中的多种类型，如 `String`。个别 `String` 值（如 "Hello"）称为该类型的“实例”。
 
 - `String` 是类型
 - `"Hello"` 是 `String` 的“实例”。
 
你现在已经创建了类型 `Int` 和 `String` 的实例。事实上，程序中创建的每个值都是其类型的实例。
 
常量 `hello` 和 `aDifferentHello` 是恰巧使用相同数据的两个 `String` 实例。

*/
let hello = "hello"
let aDifferentHello = "hello"
/*: 
 
你已经知道，实例的类型决定其包含的数据类型以及它在代码中的使用方式。
 
你还对实例执行了一些操作：将字符串加在一起、对数字执行计算等。但是，实例的作用远不止于此。
 
在此 playground 中，你将学习更多创建和使用实例的知识，包括访问值和行为。
 
接下来，我们来学习几个创建实例的新方法。

[上一页](@previous)  |  第 2 页，共 17 页  |  [下一页：创建实例](@next)
*/
