/*:
 ## 练习：身份
 
 实例值具有自己的身份到底有何意义？

 - callout(Exercise): (练习):\

 为 `myPlans` 创建变量，初始化为字符串，以描述你晚上的计划。\
为 `friendPlans` 创建第二个变量，但是将其初始化为 `myPlans`。（你的朋友在计划方面的主动性要比你低）。\
在 `friendPlans` 的声明下面，通过使用加法运算符 `+` 再添加一个活动，来更新 `myPlans`。\
检查 `myPlans` 和 `friendPlans` 的值。它们是否相同？
 */
// Create your variables here:



// Update `myPlans` here:




/*:
 - callout(Exercise): (练习):\

 创建采用字符串的函数 `addDance`，附加有关跳舞的短语（如`"然后我们跳舞！"` 或 `"但是不跳舞"`（根据你的喜好）），然后返回新字符串。\
在 `myPlans` 上调用 `addDance`，然后将结果赋给 `friendPlans`。
 */
// Define and call your function here:




/*:
 - callout(Exercise): (练习):\

 你预期 `friendPlans` 如何更改？你预期 `myPlans` 如何更改？\
 打印这两个实例来查看更改。
 */
// Check your guess by printing here:




/*:
 
 _Copyright © 2017 Apple Inc._
 
 _特此授予任何人免费获取本软件及相关文档文件（下文简称“本软件”）副本的权利，允许其无限制地处理本软件，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或出售本软件副本的权利，并允许本软件的合法用户这样做，但须符合以下条件：_
 
 _上述版权声明和本许可声明应包含在本软件的所有副本或主要部分中。_
 
 _本软件按其现状提供，不作任何明示或默示担保，包括但不限于对适销性、特定用途的适用性和不侵权的保证。在任何情况下，作者或版权所有者均不对因合同、侵权或其他由于使用或处理本软件而引起或与其相关的任何索赔、损害或其他责任负责。_
 */
//: [上一页](@previous)  |  第 17 页，共 17 页
